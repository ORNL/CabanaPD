---
icon: lucide/sigma
---

# Theoretical framework

This editor allows to directly use latex math inline like this $\cos x$

But also allows blocks like this:

$$
\cos x=\sum_{k=0}^{\infty}\frac{(-1)^k}{(2k)!}x^{2k}
$$
